import pathlib

CODE_DIR = pathlib.Path(__file__).resolve().parent.parent.parent

# change the following line if the data repository is not in the same directory as the code repository
DATA_REPO = CODE_DIR.parent / "Sridhar_Gollisch_2025_Marmoset_RGC_Responses_Naturalistic_Movies"

MAX_FLOAT_SIZE = 500
